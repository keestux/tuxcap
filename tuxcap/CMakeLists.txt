PROJECT(TUXCAP)

#include some macros from another file...
INCLUDE(${CMAKE_SOURCE_DIR}/CMakeMacros/IJMacros.txt)

SET(CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/CMakeModules" )

FIND_PACKAGE(ImageMagickLib)
IF(IMAGEMAGICKLIB_FOUND)
    MESSAGE("DBG ImageMagickLib found. ${IMAGEMAGICKLIB_INCLUDE_DIR} ${IMAGEMAGICKLIB_LINK_DIRECTORIES}")
    LINK_DIRECTORIES(${IMAGEMAGICKLIB_LINK_DIRECTORIES})
ELSE(IMAGEMAGICKLIB_FOUND)
    MESSAGE("DBG ImageMagickLib  requested but not found.")
ENDIF(IMAGEMAGICKLIB_FOUND)

FIND_PACKAGE(AudiereLib)
IF(AUDIERELIB_FOUND)
    MESSAGE("DBG AudiereLib found. ${AUDIERELIB_INCLUDE_DIR} ${AUDIERELIB_LINK_DIRECTORIES}")
    LINK_DIRECTORIES(${AUDIERELIB_LINK_DIRECTORIES})
ELSE(AUDIERELIB_FOUND)
    MESSAGE("DBG AudiereLib requested but not found.")
ENDIF(AUDIERELIB_FOUND)


SUBDIRS(demo1 demo2 demo3 demo4 demo5 hungarr lib)

SET (CMAKE_BUILD_TYPE Debug)


